<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">大德天成</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active">
          <%= link_to("Products", products_path) %>
        </li>
      </ul>
      <ul class="nav navbar-nav">
        <li class="active">
          <%= link_to("Messages", messages_path) %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_admin %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              管理员已登录
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li> <%= link_to("产品管理", admin_products_path) %> </li>
              <li> <%= link_to("信息管理", admin_messages_path) %> </li>
              <li> <%= link_to("留言管理", admin_comments_path) %> </li>
              <li> <%= link_to("分类管理", categories_path ) %></li>
              <li> <%= link_to(content_tag(:i, '登出', class: 'fa fa-sign-out'), destroy_admin_session_path, method: :delete) %> </li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<h1>Hello</h1>

<div class="container-fluid col-sm-8">
  <footer>
    <p class="text-center">Copyright ©2017 大德天成
      <br>
      Design by 攻城拔寨
    </p>
  </footer>
</div>
<div class="container-fluid col-sm-4">
  <h3>留言</h3>
  <%= simple_form_for [@comment] do |f| %>
    <div class="group">
      <%= f.input :name, label:"姓名：" %>
    </div>
    <div class="group">
      <%= f.input :tel, label:"联系方式："%>
    </div>
    <div class="group">
      <%= f.input :email, label:"邮箱地址：" %>
    </div>
    <div class="group">
      <%= f.input :description, label:"留言内容：" %>
    </div>
    <%= f.submit "发送", data: { disable_with: "Submitting..." } %>
  <% end %>
</div>
